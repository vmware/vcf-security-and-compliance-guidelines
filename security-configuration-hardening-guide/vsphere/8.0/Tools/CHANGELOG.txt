VMware vSphere Security Configuration Guide (SCG) 8.0.3 PowerCLI Toolkit
=========================================================================

CHANGELOG
---------

Version 803-20260612-01
-----------------------

Bug Fixes:
- Fixed -NoSafetyChecksExceptAppliances not bypassing safety checks in audit-vm-8.ps1 and remediate-vm-8.ps1
- Fixed Config.Etc.Issue casing to Config.Etc.issue in audit-esxi-8.ps1 and remediate-esxi-8.ps1
- Fixed missing closing parenthesis in remediate-vcenter-8.ps1 etc.issue log message
- Fixed missing MacManagementOverrideAllowed and NetworkResourcePoolOverrideAllowed in remediate-vcenter-8.ps1 portgroup policy remediation
- Fixed audit-all.ps1 to require only one of -OutputDirName or -CSVOutputDirName and to validate the CSV output directory
- Made -Name optional in audit-vcenter-8.ps1 and remediate-vcenter-8.ps1; defaults to the current vCenter connection
- Changed the VM hardware version check in audit-vm-8.ps1 to pass any version at or above vmx-17, matching the guide's revised baseline

New Features:
- Added -CSVOutputFileName parameter to all scripts for CSV output
- Added -CSVOutputDirName parameter to audit-all.ps1 for bulk CSV output
- CSV output uses Export-Csv with proper quoting and escaping

Files Modified:
- scg-common.psm1
- audit-all.ps1
- audit-vm-8.ps1
- remediate-vm-8.ps1
- audit-esxi-8.ps1
- remediate-esxi-8.ps1
- audit-vcenter-8.ps1
- remediate-vcenter-8.ps1

Version 803-20260107-01
-----------------------

Bug Fixes:
- Fixed EULA prompt accepting modifier keys (Ctrl, Alt, Shift) as valid input
- Ctrl key alone no longer triggers acceptance, allowing CTRL+C to exit properly
- Updated prompt text to "Press any character key" for clarity
- Fixed undefined variable ($value -> $localsyslog) in audit-esxi-8.ps1
- Fixed backwards remediation logic: MacChanges/ForgedTransmits now correctly set to $false
- Fixed Syslog.global.logLevel from 'info' to 'error' per SCG specification
- Fixed VCSA checks incorrectly placed inside distributed switch loop
- Removed unused $hosts variable from VM scripts
- Fixed line number references in remediation script safety warnings
- Fixed Config.HostAgent.vmacore.soap.sessionTimeout from 30 to 10 per SCG specification

Security Check Improvements:
- Added comparator-based checks (eq, ge, le) to accommodate "more secure" configurations

Refactoring:
- Created scg-common.psm1 shared module to eliminate duplicated code
- Refactored SSO policy checks to be data-driven
- Consolidated VM numeric checks into unified data structure
- Removed version check logic from all scripts

Security Enhancements:
- Updated connect.ps1 to use PSCredential instead of plaintext password
- Added error handling with try/catch blocks throughout
- Added error handling to audit-all.ps1, audit-vcenter-8.ps1, connect.ps1

Files Modified:
- scg-common.psm1 (new)
- connect.ps1
- audit-all.ps1
- audit-esxi-8.ps1
- audit-vcenter-8.ps1
- audit-vm-8.ps1
- remediate-esxi-8.ps1
- remediate-vcenter-8.ps1
- remediate-vm-8.ps1
